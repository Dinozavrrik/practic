#include <iostream>
#include <math.h>

using namespace std;

int main()
{ int k=9, k1=17, i=1, n=i;
    float w=0;
    int x;
    float z, c;
    cout<<"x=";
    cin>>x;

  z=(pow((abs(7-x)),i))/(pow((i-3),5));
  c=((pow(n,3))-8)/(n-12);
  
  if(z!=0 and c!=0)
    do 
    { w=z*c;
    i+=1;
    }
    while (i<=k and i<=k1);
    
 
    cout<<"w="<<w;

    return 0;
}


